# Full stack: livekit, redis, web, agent.
# Ollama runs OUTSIDE Docker on your PC (start it yourself: ollama serve, ollama pull gemma2).
# The agent container reaches the host's Ollama via OLLAMA_BASE_URL=http://host.docker.internal:11434/v1.
services:
  livekit:
    build:
      context: .
      dockerfile: livekit.Dockerfile
    ports:
      - "15580:15580"
      - "15581:15581"
      - "15582:15582/udp"
    restart: always
    volumes:
      - livekit_data:/var/lib/livekit

  redis:
    image: redis:7-alpine
    command: redis-server --save 20 1
    ports:
      - "16379:6379"
    restart: always
    volumes:
      - redis_data:/data

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "15500:15500"
    environment:
      - NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:15580
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      # Dev default must match keys: in livekit.yaml
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-dev-secret-0123456789abcdef-0123456789abcdef}
      - HOSTNAME=0.0.0.0
      - OLLAMA_HOST=http://host.docker.internal:11434
      - PORT=15500
    depends_on:
      - livekit
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"

  agent:
    build:
      context: .
      dockerfile: apps/agent/Dockerfile
    environment:
      - LIVEKIT_URL=ws://livekit:15580
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      # Dev default must match keys: in livekit.yaml
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-dev-secret-0123456789abcdef-0123456789abcdef}
      - OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
      - OLLAMA_MODEL=gemma2
    depends_on:
      - livekit
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - lancedb_data:/app/lancedb_data

volumes:
  livekit_data:
  redis_data:
  lancedb_data:

  # Optional: Authentik identity provider
  # To enable, uncomment the authentik service below and configure
  # AUTH_AUTHENTIK_ID / AUTH_AUTHENTIK_SECRET in your web env.
  # See docs/LIVEKIT.md for full setup guide.
  #
  # authentik:
  #   image: ghcr.io/goauthentik/server:latest
  #   command: server
  #   ports:
  #     - "9000:9000"
  #     - "9443:9443"
  #   environment:
  #     AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-change-me}
  #     AUTHENTIK_POSTGRESQL__HOST: authentik-db
  #     AUTHENTIK_POSTGRESQL__NAME: authentik
  #     AUTHENTIK_POSTGRESQL__USER: authentik
  #     AUTHENTIK_POSTGRESQL__PASSWORD: ${AUTHENTIK_DB_PASSWORD:-change-me}
  #   depends_on:
  #     - authentik-db
  #   restart: always
  #
  # authentik-db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: authentik
  #     POSTGRES_USER: authentik
  #     POSTGRES_PASSWORD: ${AUTHENTIK_DB_PASSWORD:-change-me}
  #   volumes:
  #     - authentik_db_data:/var/lib/postgresql/data
  #   restart: always
  #
  # authentik-worker:
  #   image: ghcr.io/goauthentik/server:latest
  #   command: worker
  #   environment:
  #     AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:-change-me}
  #     AUTHENTIK_POSTGRESQL__HOST: authentik-db
  #     AUTHENTIK_POSTGRESQL__NAME: authentik
  #     AUTHENTIK_POSTGRESQL__USER: authentik
  #     AUTHENTIK_POSTGRESQL__PASSWORD: ${AUTHENTIK_DB_PASSWORD:-change-me}
  #   depends_on:
  #     - authentik-db
  #   restart: always
